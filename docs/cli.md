# CLI

Core commands:

```bash
contpress count prompt.txt
contpress trim prompt.txt --max-tokens 2000
contpress compress prompt.txt --target-tokens 1000 --profile safe
contpress compact data.json
contpress report prompt.txt --budget 4000
contpress diff original.txt optimized.txt
contpress doctor
```
