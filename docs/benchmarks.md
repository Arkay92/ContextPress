# Benchmarks

Run the sample benchmark prompts:

```bash
contpress benchmark examples/benchmark_prompts/
```

The output includes prompt count, average original tokens, average optimized
tokens, average reduction, and average processing time.
