# Provider Examples

ContextPress runs before provider calls. These examples show the intended shape:
optimize with `contpress`, then call OpenAI, Anthropic, Ollama, LiteLLM, Groq, or
Mistral directly with the optimized text.
